# VARIABLES  - containers for storing values / the labels to memory locations using which we can access values.
#            - python has no command for declaring variables........so no need to declare variables in python


# Rules:
# A variable name must start with a letter or the underscore character
# A variable name cannot start with a number
# A variable name can only contain alpha-numeric characters and underscores (A-z, 0-9, and _ )
# A variable name cannot contain special symbols and space
# Variable names are case-sensitive (age, Age and AGE are three different variables)


#Legal variable names:
# =====================
myvar = "John"
my_var = "John"
_my_var = "John"
myVar = "John"
MYVAR = "John"
myvar2 = "John"

#Illegal variable names:
# =======================
# 2myvar = "John"
# my-var = "John"
# my var = "John"

# Valid variable names:
var=10
print(var)

var="hello"
print(var)

Var="hello"
print(Var)

VAR="hello"
print(VAR)

VAR2="hello"
print(VAR2)

_Var_2="hello"
print(_Var_2)

# Invalid variable names:         # 2var="hello"     ,         VA R2="hello"